import { CustomhilighterDirective } from './customhilighter.directive';

describe('CustomhilighterDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomhilighterDirective();
    expect(directive).toBeTruthy();
  });
});
